[tool.poetry]
name = "splunk_add_on_ucc_framework"
version = "0.1.0"
description = ""
authors = ["rfaircloth-splunk <rfaircloth@splunk.com>"]
exclude = ["splunk_add_on_ucc_framework/UCC-UI-lib"]

[tool.poetry.dependencies]
python = "^3.7"
solnlib = '>=2.0.8a3'
splunk-sdk = "^1.6"
jinja2 = "^2"
mako= "^1"
munch= "^2"
lxml = "^4.5"
future = "^0"

[tool.poetry.dev-dependencies]
pytest = "^6"

[tool.poetry.scripts]
ucc-gen = "splunk_add_on_ucc_framework:main"
build-ucc = "splunk_add_on_ucc_framework:build_ucc"
install-libs = "splunk_add_on_ucc_framework:install_requirements"
requires = ["setuptools >= 40.6.0", "wheel"]
build-backend = "setuptools.build_meta"

