module.exports = function(grunt) {
    'use strict';

    if ("CommonComponent" == this.learn('initConfig.buildinfo').type) {
        grunt.log.writeln("secret environment info: " + JSON.stringify(this.learn('initConfig.environments'), null, 2));
        return this.learn('initConfig.environments');
    }
};
