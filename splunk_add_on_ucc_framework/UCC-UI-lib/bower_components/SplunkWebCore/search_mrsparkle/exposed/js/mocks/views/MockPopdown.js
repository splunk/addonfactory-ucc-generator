define([
            'module',
            'mocks/views/MockView'
        ],
        function(
            module,
            MockView
        ) {

    return MockView.extend({

        show: function() {},
        hide: function() {}

    });

});