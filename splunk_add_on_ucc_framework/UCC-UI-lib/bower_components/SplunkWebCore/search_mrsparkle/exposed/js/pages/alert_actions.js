define(['routers/AlertActions', 'util/router_utils'], function(AlertActionsRouter, router_utils) {
    var alertActionsRouter = new AlertActionsRouter();
    router_utils.start_backbone_history();
});
