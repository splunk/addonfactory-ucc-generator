[module]
# The JavaScript name of the module
className = Splunk.Module.Export

# The module class to subclass from
superClass = Splunk.Module.DispatchingModule

description = Displays a link that launches the job export utility.

[param:exportType]
required = True
